node_modules/
dist/
# CMake 外付けビルド（compile_commands.json もここに出る）
build/
.DS_Store
**/.DS_Store
# 開発時に modeling からコピーされる（約20MB）。リポジトリには含めない
public/modeling/Wani_game.glb
medea-pipeline/data/hand-dataset.json

# Environment variables (secrets/local settings)
.env
**/.env
.env.*
**/.env.*
!.env.example
!**/.env.example

# Local .terraform directory (contains provider plugins and modules)
.terraform/

# .tfstate files (can contain sensitive data like passwords)
*.tfstate
*.tfstate.backup
*.tfstate.lock.info

# Terraform execution plans (can contain sensitive data)
tfplan*
*.tfplan
*.tfstate
*.tfstate.*

# Terraform local vars (sensitive)
terraform.tfvars
**/terraform.tfvars
